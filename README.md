# server-monitoring-engine
Real-time monitoring engine which continuously tracks theavailability and response times of servers. Implemented scheduledhealth checks using IP/hostname inputs and dynamically updatedserver status in an embedded H2 database.
